{
  "name": "@jsdevtools/tuneable",
  "version": "0.1.0",
  "description": "Wraps components and makes their props tuneable via dispatching new prop values.",
  "main": "dist/bundle.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "webpack --watch --config webpack.config.js",
    "prepare": "yarn run build",
    "build": "webpack --config webpack.config.js",
    "test": "jest",
    "clean": "shx rm -f yarn-error.log && shx rm -rf dist"
  },
  "peerDependencies": {
    "prop-types": "^15.7.2",
    "react": "^16.8.6"
  },
  "dependencies": {
    "webpack": "^4.30.0"
  },
  "devDependencies": {
    "@jsdevtools/dev-dependencies": "*",
    "prop-types": "^15.7.2",
    "react": "^16.8.6"
  }
}
